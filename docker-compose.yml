services:
  server:
    image: ghcr.io/jcollingwood/jc-kotlin-template:96ce78a9f4661bf971d29b187cd2a23a3d2cb601
    env_file:
      - .env
    environment:
      - PORT=1111
    network_mode: host
    restart: unless-stopped
    volumes:
      - sqlite_data:/app/db/

volumes:
  sqlite_data:
    driver: local


